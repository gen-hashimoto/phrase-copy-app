# 02 Screen Design

## 概要

本ドキュメントでは、Phase1で実装する画面の構成および主要なUI要素を定義する。

---

## 画面一覧

| 画面名                                       | 説明                                     |
| -------------------------------------------- | ---------------------------------------- |
| トップ画面: フレーズ一覧画面（ゲストモード） | フレーズの表示・コピー・管理（最大10件） |
| ログイン画面                                 | メールアドレス入力、Magic Link送信       |
| フレーズ一覧画面（ログインモード）           | フレーズの表示・コピー・管理（最大50件） |

---

## 1. トップ画面: フレーズ一覧画面（ゲストモード）

### ゲスト画面イメージ

![top-screen](./images/top-screen.png)

### ゲストUI要素

- タイトル
- ログインボタン
- フレーズリスト（最大10件）
- 各フレーズのコピーボタン
- 各フレーズの削除ボタン
- Copy All ボタン
- フレーズ追加ボタン（+ ボタン）
- 使用数表示（例：8 / 10 used）
- Easy Import ボタン（テキスト形式インポート・未実装）
  - テキスト入力エリア（複数行）
  - Append（追加） ボタン
  - Overwrite（上書き） ボタン
- Append ボタン押下時のダイアログ警告
- Overwrite ボタン押下時のダイアログ警告

### ゲスト挙動

- ログイン不要で利用できる
- フレーズを10件まで登録可能
- ログイン前はDBに保存せず、ReactのuseStateでフレーズを保持する
- コピーボタンでコピー
- Copy Allで全フレーズを改行区切りでコピー
  - コピーしたものを、メモアプリなどに簡易バックアップ可能
- フレーズをダブルクリックで編集可能
  - 編集可能になると、フレーズ部分がテキストボックスに変化（背景色が変化）
  - コピーボタンが編集確定OKボタン、編集Cancelボタンに変化
  - 登録できるフレーズに改行は想定していない。改行を許容すると、改行区切りでインポートする、という仕様の前提が崩れてしまう。
- フレーズ追加ボタン（+ボタン）を押下すると、最下部に行が追加されて編集状態となり、フレーズを追加できる。
  - フレーズが10件に達するとフレーズ追加ボタンは無効化される。
  - フレーズ追加ボタンの右にフレーズ数上限、使用済みのフレーズ数が表示される。（例: 8 / 10 used）
- ジャンル機能、検索機能は提供しない
- Easy Import（テキスト形式インポート）は未実装。実装時は以下の仕様とする
  - 改行区切りのテキストからインポートできる
  - インポート方法は2通り選択可能。アコーディオンボタンを押すと、テキストエリアとボタンが二つ表示される（赤点線枠内）
    - Appendボタンによる追加インポート
    - Overwriteボタンによる上書きインポート
  - Append、Overwrite押下時にはダイアログで警告が出る
  - インポートの後に10件を超える場合はバリデーションエラーが表示される
- テキスト形式エクスポート、広告表示は未実装

### ゲスト画面のスマホ対応

- Webアプリのためスマホ向けにレスポンシブ対応
- ボタンサイズに注意
- フレーズの編集について、スマホの場合はダブルタップできるように留意すること

### ゲスト実装想定

- フロントエンドはReactでの実装を想定しており、ログイン前はDBに保存せず、useStateの機能を使用してフレーズを保持する想定

---

## 2. ログイン画面

### ログイン画面イメージ

![login-screen](./images/login-screen.png)

### ログインUI要素

- メールアドレス入力フィールド
- Magic Link送信ボタン
- ヘッダータイトル: トップ画面へのリンク

### ログイン挙動

- メールアドレス入力後、Magic Linkを送信 // TODO: api/auth
- メール送信済み画面表示
- メール内のリンクをクリック
  - アカウントなしの場合は、アカウントが作成された内容のコメントをメール内に記載
- 認証成功: ログイン完了 // TODO: api/auth
- 認証失敗
  - トークン期限切れ → 「リンクの有効期限が切れています」
  - 不正トークン → 「無効なリンクです」
  - 再ログイン導線を表示

---

## 3. フレーズ一覧画面（ログインモード・無料）

トップ画面: フレーズ一覧画面（ゲストモード）との差分のみ表示

### ログインモード画面イメージ

![phrase-list-screen](./images/phrase-list-screen.png)

### ログインモードUI要素

- ログアウトボタン
- ジャンル管理UI
- 検索バー（未実装・追加予定）
- フレーズ移動ボタン（矢印ボタン）

### ログインモード挙動

- 利用にはアカウント作成が必要
- フレーズは最大50件まで登録可能
- ジャンル数は無制限
- フレーズはクラウドDBに保存され、再度ログインした時に前回と同じ状態が読み込まれる
- 複数端末で同じフレーズを同期できる
- 複数行フレーズを登録できる
- 次の操作でデータがDBに保存され、再度フレーズ一覧が読み込まれる( // TODO: api/phrase )
  - フレーズ追加ボタン( // TODO: api/phrase )
  - フレーズ編集( // TODO: api/phrase )
  - フレーズ削除ボタン( // TODO: api/phrase )
  - フレーズ移動ボタン( // TODO: api/phrase )
- ログアウトボタン押下でトップ画面: フレーズ一覧画面（ゲストモード）に戻る
- 検索機能は未実装。実装時は以下の仕様とする
  - 部分一致
  - リアルタイムフィルタ（フロントエンド、React実装想定）
  - 大文字小文字無視
- CSVエクスポート、CSVインポート、広告表示は未実装
- 今後追加される基本機能を利用可能
- フレーズ移動ボタンの矢印を押すと、矢印の方向に応じてフレーズの順番が変えられる( // TODO: api/phrase )
- フレーズが50件に達するとフレーズ追加ボタンは無効化される

---

## 4. フレーズ一覧画面（プレミアムモード・有料・将来用）

ログインモード（無料）との差分のみ表示

### プレミアムモード挙動

- ログインモード（無料）の全機能を利用できる
- フレーズ登録数は無制限
- 広告を非表示にする

---

## 5. 共通仕様

### コピー機能

- クリックでクリップボードにコピー
- コピー成功時のフィードバック
  - ボタン横に「Copied!」表示（1秒）
  - コピーされたフレーズが光るようにハイライトされる
  - 1秒程度経過するとボタン、フレーズ表示ともに元に戻る

### キーボード操作

- Enter：決定
- Esc：キャンセル
- Opt+F / Alt+F: 検索機能の実装後、検索バーにフォーカス

---

## 6. 備考

- UIはシンプルさを重視する
- Phase1では最低限の機能に絞る

# 設計ドラフトと実装のメモ

| 項目 | 設計（02_screen-design） | 本 sandbox / 現行コード |
|------|-------------------------|-------------------------|
| 新規追加 | + ボタンで最下行が編集状態 | step-04。現状は Card「新規作成」フォーム |
| 編集開始 | ダブルクリック（スマホはダブルタップ） | step-02。現状は「編集」ボタン |
| 編集時の操作 | Copy → OK / Cancel | step-03。現状は「保存（PUT）/ キャンセル」 |
| フレーズ本文 | 改行なし想定 | step-06 で textarea バリデーション例 |
| DB 列名 | 将来 `phrase` | step-05 では既存 `content` を維持（リネームは任意） |
| 未ログイン | useState、最大 10 件 | 未実装。本トピックの step 6 に参照のみ |
